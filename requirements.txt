# Dependencias dos 10 pipelines (YouTube API + lakehouse local)
# Instale uma unica vez no ambiente virtual:
#   python -m venv .venv
#   source .venv/bin/activate        (Windows: .venv\Scripts\activate)
#   pip install -r requirements.txt

pandas>=2.0
polars>=1.0
duckdb>=1.0
pandera>=0.20          # expoe o namespace pandera.pandas usado nos notebooks
youtube-transcript-api>=1.0   # API v1.x: YouTubeTranscriptApi().fetch()
pyarrow>=15.0
